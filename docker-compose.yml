services:
  postgres:
    image: postgres:16
    container_name: career-mentor-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: career-mentor_db
    ports:
      - "5433:5432"
